import { BreweryFilterPipe } from './brewery-filter.pipe';

describe('BreweryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BreweryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
